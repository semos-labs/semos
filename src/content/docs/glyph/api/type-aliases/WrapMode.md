---
title: 'WrapMode'
---
```ts
type WrapMode = "wrap" | "truncate" | "ellipsis" | "none";
```

Text wrapping mode for [Style.wrap](../interfaces/Style.md#wrap).
- `"wrap"` — soft-wrap at container edge
- `"truncate"` — cut off silently
- `"ellipsis"` — cut off with `…`
- `"none"` — no wrapping
